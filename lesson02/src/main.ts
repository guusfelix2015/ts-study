let myName: string = 'Max';
let meaningOfLife: number;
let isLoading: boolean;
let album: string | number;

myName = 'Gustavo';
meaningOfLife = 42;
isLoading = true;
album = 'Van Halen';

const sum = (a: number, b: string) => a + b;

let postId: string | number;
let isActived: number | boolean;

let re: RegExp = /\w+/g;
